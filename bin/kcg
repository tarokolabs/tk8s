#!/bin/bash
[ "$1" == "" ] && echo "kcg <cluster>" && exit 1

export cn=${1}

[ ! -f ~/tk/conf/${cn}.conf ] && echo "~/tk/conf/${cn}.conf not found" && exit 1

source ~/tk/conf/${cn}.conf
([ "$LCTN" == "" ] || [ "$TKIND" == "" ] || [ "$NTP" == "" ]) && echo "~/tk/conf/${cn}.conf error" && exit 1

case "$NTP" in
  "Internal")
     sudo podman network ls | grep $1 &>/dev/null
     [ "$?" != "0" ] && echo "${cn} cluster not exist" && exit 1

     sudo podman ps | grep "${cn}-" | grep 'Up' &>/dev/null
     [ "$?" == "0" ] && echo "pls stop ${cn} first (kco ${cn})" && exit 1

     read -p "Are you sure ? " yn
     [ "$yn" != "YES" ] && exit 1

     KVER=$(cat /opt/zfs/${cn}/init-config.yaml | grep 'kubernetesVersion: ' | cut -d' ' -f2)

     sudo rm -r /opt/zfs/${cn}-bak-${KVER} &>/dev/null
     sudo mkdir -p /opt/zfs/${cn}-bak-${KVER}/ && sudo chmod -R 777 /opt/zfs/${cn}-bak-${KVER}/
     mdir=$(sudo podman info | grep volumePath | cut -d':' -f 2 | tr -d ' ')

     for c in ${LCTN}
     do
       ctn=$(echo $c | cut -d':' -f2)

       sudo podman images | grep "${ctn}:v${KVER}" &>/dev/null
       [ "$?" == "0" ] && sudo podman rmi -f "${ctn}:v${KVER}" &>/dev/null

       sudo podman commit ${ctn} ${ctn}:v${KVER} &>/dev/null
       if [ "$?" == "0" ]; then
         sudo podman save ${ctn}:v${KVER} > /opt/zfs/${cn}-bak-${KVER}/${ctn}-v${KVER}.tar
         echo "${ctn}:v${KVER} image backup ok"

         sudo podman inspect --format='{{json .Config.CreateCommand}}' ${ctn} | jq -c | tr -d '[]' > /tmp/${ctn}.txt
         if [ "$?" == "0" ]; then
            tcmd=$(sed -e 's/\",\"/ /g' /tmp/${ctn}.txt | tr -d '"')
            tstr="s/quay.io\/cloudwalker\/taroko/localhost\/${ctn}/g"
            cmd=$(echo $tcmd | sed -e $tstr)
            echo "sudo $cmd" > /opt/zfs/${cn}-bak-${KVER}/${ctn}-v${KVER}.sh
            sudo chmod +x /opt/zfs/${cn}-bak-${KVER}/${ctn}-v${KVER}.sh
            echo "${ctn}:v${KVER} rebuild script ok"

            sudo tar cvf ${ctn}-volume.tar ${mdir}/${ctn}-volume/ &>/dev/null
            [ "$?" == "0" ] && sudo mv ${ctn}-volume.tar /opt/zfs/${cn}-bak-${KVER}/ && echo "${ctn}-volume.tar ok"
         fi
       fi
     done
     sudo tar cvf ${cn}.tar /opt/zfs/${cn}/ &>/dev/null
     [ "$?" == "0" ] && sudo mv ${cn}.tar /opt/zfs/${cn}-bak-${KVER}/ && echo "${cn}.tar ok"
     echo ""
     echo "${cn} backup ok"

     ;;
  "External")
     for c in ${LCTN}
     do
       cnn=$(echo $c | cut -d':' -f1)
       cif=$(echo $c | cut -d':' -f2)
       nip=$(echo $c | cut -d':' -f3)
       cip=$(echo $c | cut -d':' -f4)
       ctn=$(echo $c | cut -d':' -f5)
       cm=$(echo $c | cut -d':' -f6)
       cc=$(echo $c | cut -d':' -f7)

     done
     ;;
  *)
     echo "Netwrok Type Error"
     exit 1
     ;;
esac
